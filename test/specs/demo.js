/*
describe("Task 5", () => {
  it("check every item have drop down menu", () => {
    //
    browser.url("https://github.com/");
    const menu = $$(
      "details[class='HeaderMenu-details details-overlay details-reset width-full']"
    );
    menu.forEach((item) => {
      item.moveTo();
      item.click();
      browser.pause(4000);
    });
  });
});
// */
