const signupPage = require("../page-objects/signup.page");

/*
describe("Signup page", () => {
  before(() => {
    signupPage.load();
  });

  //
  it("should load properly", () => {
    // check that page locator exist
    console.log(`The URL is: ${browser.getUrl()}`);
    expect(signupPage.$username).toBeExisting();
    expect(signupPage.$email).toBeExisting();
    expect(signupPage.$password).toBeExisting();
  });
});
*/
